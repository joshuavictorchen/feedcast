# Silas Feeding Forecast

**{{ date_display }}** · {{ age_days }} days old · Cutoff: {{ cutoff_display }}

## Next Feeds

![Featured Forecast](schedule.png)

## Model Trajectories

![Forecast Trajectories](spaghetti.png)

## Prior Run Retrospective

{% if not retrospective.available -%}
{% if retrospective.same_dataset -%}
No new actuals since the prior run
(same dataset: `{{ retrospective.dataset_id_short }}`).
{% else -%}
No prior run available yet.
{% endif -%}
{% else -%}
Comparing prior run `{{ retrospective.prior_run_id }}` predicted episodes
against actual feeding episodes observed in the current export
(observed horizon:
{{ "%.1f" | format(retrospective.observed_horizon_hours) }}h,
coverage: {{ retrospective.coverage_display }}).

| Model | Score | Count | Timing | Episodes (Pred/Actual/Matched) | Status |
| ----- | ----- | ----- | ------ | ------------------------------ | ------ |
{% for result in retrospective.results -%}
| {{ result.name }} | {{ result.score_display }} | {{ result.count_display }} | {{ result.timing_display }} | {{ result.feeds_display }} | {{ result.status }} |
{% endfor %}
{% if retrospective.observed_horizon_hours < 24 %}
Scores are normalized to the observed window. Coverage shows how much of
the 24-hour horizon has actually resolved so far.
{% endif -%}
{% endif %}

## Historical Retrospective Accuracy

{% if historical_accuracy -%}
Aggregated from stored prior-run retrospectives. These scores
reflect the model versions that made those earlier predictions.

| Model | Comparisons | Full 24h Runs | Mean Score | Mean Count | Mean Timing | Avg Coverage |
| ----- | ----------- | ------------- | ---------- | ---------- | ----------- | ------------ |
{% for row in historical_accuracy -%}
| {{ row.name }} | {{ row.comparisons }} | {{ row.full_horizon }} | {{ row.score_display }} | {{ row.count_display }} | {{ row.timing_display }} | {{ row.coverage_display }} |
{% endfor %}
{% else -%}
No completed retrospective history yet.
{% endif %}

## Methodologies

{% for row in methodologies %}

### {{ row.title }}

{{ row.body }}
{% endfor %}

---

*Export: `{{ source_file }}` · Dataset: `{{ dataset_id_short }}`
· Commit: `{{ git_commit_display }}`
· Generated: {{ generated_at }}*
