# Model Tuning

This prompt is a Phase 1 placeholder.

When fully implemented, it will review one model using:

- model slug: `{{model_slug}}`
- model directory: `{{model_dir}}`
- export: `{{export_path}}`
- last retrospective scores: `{{last_retro_scores}}`
- research hub path: `{{research_hub_path}}`

The pipeline does not invoke this skill yet. Replace this scaffold during
Phase 3 with the real tuning instructions.
